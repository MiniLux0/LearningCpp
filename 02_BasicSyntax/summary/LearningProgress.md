# Learning Progress — LearningCpp

## 01_GettingStarted
- **L01_IntroducingCpp.cpp** — Empty — no content yet
- **L02_ScreenResolution.cpp** — Empty — no content yet
- **L03_SetupAndInstallation.cpp** — Empty — no content yet
- **L04_HelloWorld.cpp** — Basic C++ program demonstrating `cout` to output strings and newlines with `\n`
- **L05_OutputtingText.cpp** — Simple `cout` statements printing user info with string concatenation

## 02_BasicSyntax
- **L06_Variables.cpp** — Demonstrates variable declarations (double, char, bool, int), arithmetic, and basic conditional logic
- **L07_Strings.cpp** — String declaration, concatenation with the `+` operator, and `cout` output
- **L08_UserInput.cpp** — Reading user input with `cin` for string and integer types
- **L09_BinaryNumbers.cpp** — Notes-only file covering binary system, bits/bytes, variable memory sizes, and overflow
- **L10_IntegerTypes.cpp** — Using `<limits>` header and `sizeof` to display min/max values and byte sizes for int, short, long, and long long
- **L11_FloatingPointTypes.cpp** — Empty — no content yet
- **L12_CharAndBool.cpp** — Empty — no content yet
- **L13_If.cpp** — Empty — no content yet
- **L14_IfElse.cpp** — Empty — no content yet
- **L15_IfElseIfElse.cpp** — Empty — no content yet
- **L16_ComparingFloats.cpp** — Empty — no content yet
- **L17_Conditions.cpp** — Empty — no content yet
- **L18_WhileLoops.cpp** — Empty — no content yet
- **L19_DoWhileLoops.cpp** — Empty — no content yet
- **L20_ForLoops.cpp** — Empty — no content yet
- **L21_BreakAndContinue.cpp** — Empty — no content yet
- **L22_Arrays.cpp** — Empty — no content yet
- **L23_MultidimensionalArrays.cpp** — Empty — no content yet
- **L24_SizeofArrays.cpp** — Empty — no content yet
- **L25_SizeofMultidimensional.cpp** — Empty — no content yet
- **L26_Switch.cpp** — Empty — no content yet

## Coding Notes
- Uses `using namespace std;` consistently — acceptable for learning but avoid in production code or headers
- Prefers `\n` over `std::endl` — good performance habit that avoids unnecessary buffer flushing
- Always includes `return 0;` in `main()` — correct practice for standard C++ compliance
- Variable names are descriptive and meaningful (e.g., `weightCatLuna`, `numberCats`) — strong naming sense
- Mixes practical code with theoretical notes (e.g., L09 binary concepts) — shows thorough study approach
